# Dev Dockerfile — backend only (Express + node --watch)
FROM node:18-alpine
WORKDIR /app/server
COPY server/package*.json ./
RUN npm ci
COPY server/ .
ENV NODE_ENV=development
ENV PORT=3001
EXPOSE 3001
CMD ["node", "--watch", "server.js"]
